export enum HoldAuditAction {
  PLACED = 'placed',
  READY = 'ready',
  FULFILLED = 'fulfilled',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
  PRIORITY_CHANGED = 'priority_changed',
  COPY_RETURNED = 'copy_returned',
}
