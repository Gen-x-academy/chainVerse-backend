name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Announce deployment
        run: echo "Deploy pipeline triggered for chainVerse-backend"
